# Database
DOCKER_DB_ROOT_PASSWORD=${DOCKER_DB_ROOT_PASSWORD}
DOCKER_SPRING_DB_PASSWORD=${DOCKER_SPRING_DB_PASSWORD}

# nginx
DOMAIN=your-domain.com
NGINX_INDEX=page.html
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
